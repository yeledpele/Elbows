namespace Elbows.Enums
{
    /// <summary>
    /// The EventPanelType enumeration provides the type values for the game's
    /// event panels which are found within the queue event system.
    /// </summary>
    public enum EventPanelType
    {
        Left = -1,
        Center = 0,
        Right = +1,
    }
}
